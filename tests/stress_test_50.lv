fn main() {
    mut x = 1;
    match x {
        1 => match x {
            1 => match x {
                1 => @println(1),
                _ => @println(0)
            },
            _ => @println(0)
        },
        _ => @println(0)
    }
}
